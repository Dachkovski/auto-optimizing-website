# Auto-Monetization Project Roadmap

## Phase 1: The Foundations (Completed)
- [x] Clone Darwin Engine sandbox.
- [x] Create `window.darwin.requestPayment()` mockup in sandbox renderer.
- [x] Adjust `src/lib/evolution.ts` system prompt to prioritize revenue generation.
- [x] Modify `scoring.ts` to apply a 10x multiplier to every $1 of revenue.

## Phase 2: Payment Infrastructure
- [ ] Implement Stripe API server-side endpoint (`/api/checkout`).
- [ ] Connect `requestPayment` to real Stripe Checkout flows (or payment elements).
- [ ] Set up Stripe Webhooks to accurately log `payment_completed` into the D1 Database.

## Phase 3: Persistence & Value Exchange
- [ ] Build a key-value API so the AI can securely store what a user has purchased.
- [ ] Expose `window.darwin.saveState(key, value)` and `window.darwin.getState(key)` to the AI.
- [ ] Create UI inside the sandbox to indicate "Premium unlocked" states across page reloads.

## Phase 4: Analytics & Dashboarding
- [ ] Update `/insights` to show public revenue generated by the entity over time.
- [ ] Update `/admin` with deep financial metrics, ARPU (Average Revenue Per User), and highest-earning generations.
- [ ] Implement a "Withdraw" mechanism for the host.
